language: ruby
rvm:
  - 2.4.1

python:
  - 3.6.4

install: gem install jekyll
script: jekyll build

before_deploy:
  - gem install jekyll
  - pip install --upgrade s3cmd

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master
