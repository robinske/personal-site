{% for node in pages_list %}
    {% if page.url == nodel.url %}
      <li class="menu-item current">
      <a href="{{node.url}}">{{node.title}}</a>
      </li>
    {% else %}
      <li class="menu-item">
      <a href="{{node.url}}">{{node.title}}</a>
  {% endif %}
{% endfor %}
